# These scripts/assets run on the Linux head node — always check out with LF.
* text=auto eol=lf

# Binary artifacts — never apply EOL/text normalization.
*.pdf  binary
*.png  binary
*.jpg  binary
*.woff2 binary
